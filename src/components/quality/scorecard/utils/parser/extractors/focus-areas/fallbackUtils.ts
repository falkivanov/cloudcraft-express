
/**
 * Fallback utilities for focus areas
 */

/**
 * Return fallback focus areas when extraction fails
 */
export function getFallbackFocusAreas() {
  return ['DNR DPMO', 'Delivery Completion Rate', 'Contact Compliance'];
}
